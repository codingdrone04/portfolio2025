name: Deploy to Lilypad

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Deploy
        run: |
          cd ~/apps/portfolio
          git pull origin main
          docker compose up -d --build
