services:
  portfolio:
    build: .
    container_name: portfolio
    # Bound to localhost only — public access goes through the Cloudflare
    # Tunnel (which reaches localhost:3000), so no need to expose on the LAN.
    ports:
      - "127.0.0.1:3000:3000"
    restart: unless-stopped
